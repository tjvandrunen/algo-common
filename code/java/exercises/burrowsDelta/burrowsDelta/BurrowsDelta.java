package burrowsDelta;

/**
 * Placeholder class for method burrowsDelta to compute the
 * "Delta" (devised by John Burrows) between two texts
 * 
 * Algorithmic Commonplaces
 * @author Thomas VanDrunen
 */
public class BurrowsDelta {

    /**
     * Compute the delta between two texts. The provided vocabulary gives the
     * set of word types that we count in each text---all other tokens in
     * the texts are ignored.
     * @param text1 
     * @param text2
     * @param vocabulary The set of word types that we are counting in each text.
     * @return
     */
    public static int burrowsDelta(Iterable<String> text1, Iterable<String> text2, Set<String> vocabulary) {
        throw new UnsupportedOperationException();
    }

}
